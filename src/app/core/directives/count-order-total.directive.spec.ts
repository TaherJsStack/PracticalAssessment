import { CountOrderTotalDirective } from './count-order-total.directive';

describe('CountOrderTotalDirective', () => {
  it('should create an instance', () => {
    const directive = new CountOrderTotalDirective();
    expect(directive).toBeTruthy();
  });
});
