import { OrderUserNamePipe } from './order-user-name.pipe';

describe('OrderUserNamePipe', () => {
  it('create an instance', () => {
    const pipe = new OrderUserNamePipe();
    expect(pipe).toBeTruthy();
  });
});
